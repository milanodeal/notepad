.wrapper_with_padding
#note.clearfix
- unless @notes.blank?
  %a{href: (url_for [note])}
  .note
  %p.title= note.title
  %p.date= time_ago_in_words(note_created_at)

- else

- @note.each do |note|
  %h2= link_to note.title, note
  %p= time_ago_in_words(note.created_at)

%button= link_to "New Note", new_note_path(@note)
